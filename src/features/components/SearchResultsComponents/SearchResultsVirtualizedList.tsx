import { FC, memo } from "react";

type Props = {
  props: string
}

const SearchResultsVirtualizedList: FC = (props) => {
  return (
    <>
      <div></div>
    </>
  )
};

export default memo(SearchResultsVirtualizedList);