/**
 * Splits a word by space.
 * @param str - the string parameter.
 * @returns An array of words separated by white space.
 */
const splitBySpace = (str: string) => str.split(/\s+/u);

export default splitBySpace;
